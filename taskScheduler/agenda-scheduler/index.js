// import Agenda from 'agenda';

// const agenda = new Agenda({ db: { address: process.env.MONGO_URI } });

// agenda.define('log a message', async job => {
//   console.log('✅ Agenda Task Executed at', new Date().toLocaleTimeString());
// });

// (async function () {
//   await agenda.start();
//   await agenda.schedule(new Date(Date.now() + 10000), 'log a message'); // 10 sec later
// })();
